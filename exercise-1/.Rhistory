# Load the `shiny` package (install it in the R terminal if you haven't already)
install.packages("shiny")
# Load the `shiny` package (install it in the R terminal if you haven't already)
#install.packages("shiny")
library(shiny)
runApp()
runApp()
runApp()
